name: Telegram News Digest

on:
  schedule:
    # הסקריפט בודק שעון ישראל (Asia/Jerusalem) ושולח רק ב-07:00 ו-20:00
    - cron: "0 4 * * *"   # 07:00 ישראל בשעון קיץ (UTC+3)
    - cron: "0 5 * * *"   # 07:00 ישראל בשעון חורף (UTC+2)
    - cron: "0 17 * * *"  # 20:00 ישראל בשעון קיץ (UTC+3)
    - cron: "0 18 * * *"  # 20:00 ישראל בשעון חורף (UTC+2)
  workflow_dispatch:       # הפעלה ידנית לבדיקות

jobs:
  send-digest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      # שחזור קובץ הזכרון מה-cache
      - name: Restore memory cache
        uses: actions/cache@v4
        with:
          path: seen_news.json
          key: seen-news-${{ github.run_id }}
          restore-keys: |
            seen-news-

      - name: Run digest
        env:
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          TELEGRAM_SESSION: ${{ secrets.TELEGRAM_SESSION }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          MY_TELEGRAM_ID: ${{ secrets.MY_TELEGRAM_ID }}
        run: python digest.py

      # שמירת קובץ הזכרון בחזרה ל-cache
      - name: Save memory cache
        if: always()
        uses: actions/cache@v4
        with:
          path: seen_news.json
          key: seen-news-${{ github.run_id }}
